{
  "id": "bun",
  "version": "1.0.0",
  "name": "Bun",
  "description": "Installs Bun - Fast all-in-one JavaScript runtime with built-in bundler, transpiler, test runner, and package manager",
  "documentationURL": "https://github.com/amondnet/devcontainer-features/tree/main/src/bun",
  "options": {
    "version": {
      "type": "string",
      "proposals": ["latest", "canary", "1.1", "1.0"],
      "default": "latest",
      "description": "Select or enter a Bun version to install (e.g., latest, canary, 1.1.0, bun-v1.0.0)"
    }
  },
  "containerEnv": {
    "BUN_INSTALL": "/usr/local",
    "PATH": "/usr/local/bin:${PATH}"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "oven.bun-vscode"
      ],
      "settings": {
        "github.copilot.chat.codeGeneration.instructions": [
          {
            "text": "This dev container includes Bun runtime for fast JavaScript/TypeScript development with built-in bundler, transpiler, and test runner."
          }
        ]
      }
    }
  }
}
